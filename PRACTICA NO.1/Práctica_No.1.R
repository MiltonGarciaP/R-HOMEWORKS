######### Practica No.1 #########

## 1. Crear un vector numerico con valores enteros de 5 elementos

vector_enteros <- c(1, 2, 3, 4, 5)
#cat(vector_enteros)

## 2. Crear un vector numerico con valores decimales de 5 elementos

vector_decimales <- c(1.5, 2.3, 3.0, 4.7, 5.2)
#cat(vector_decimales)

## 3. Crear un vector tipo cadena de 5 elementos.

vector_cadenas <- c("Hola", "Mundo", "en", "R", "!")
#cat(vector_cadenas)
## 4. Crear un vector bolenano de 5 elementos.

vector_booleano <- c(TRUE, FALSE, TRUE, TRUE, FALSE)
#cat(vector_booleano)
## 5. Verficar el tipo de cada vector creado anteriormente

cat("Tipo de vector_enteros:", class(vector_enteros), "\n")
cat("Tipo de vector_decimales:", class(vector_decimales), "\n")
cat("Tipo de vector_cadenas:", class(vector_cadenas), "\n")
cat("Tipo de vector_booleano:", class(vector_booleano), "\n")




